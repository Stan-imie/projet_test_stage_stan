lexik_jwt_authentication:
    secret_key: '%env(resolve:JWT_SECRET_KEY)%'
    public_key: '%env(resolve:JWT_PUBLIC_KEY)%'
    pass_phrase: '%env(JWT_PASSPHRASE)%'
    token_ttl : 3600

# lexik_jwt_authentication :
#      secret_key : '%kernel.project_dir%/config/jwt/private.pem'  # requis pour la création de jeton
#     public_key : '%kernel.project_dir%/config/jwt/public.pem'   # requis pour la vérification de jeton
#     pass_phrase : 'votre_secret_passem'  # requis pour la création de jeton, l'utilisation d'une variable d'environnement est recommandée
#     token_ttl : 3600
